import { NgdropfileDirective } from './ngdropfile.directive';

describe('NgdropfileDirective', () => {
  it('should create an instance', () => {
    const directive = new NgdropfileDirective();
    expect(directive).toBeTruthy();
  });
});
