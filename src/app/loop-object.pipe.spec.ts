import { LoopObjectPipe } from './loop-object.pipe';

describe('LoopObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new LoopObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
